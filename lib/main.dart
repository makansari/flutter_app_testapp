import 'package:flutter/material.dart';
import 'package:flutter_app_testapp/SecondScreen.dart';

void main() {
  runApp(
    SecondScreen()
    );

     /* title: "MY TITLE",
      home: Scaffold(
        appBar: AppBar(title: Text("First Screen"),),
        body: Material(
          color: Colors.lime,
          child: Center(
            child: Text("Welcome to Android", textDirection: TextDirection.ltr,
            style: TextStyle(color: Colors.deepOrange, fontSize: 30.0),
            ),
          ),
        ),
      ),

    )
*/

}

